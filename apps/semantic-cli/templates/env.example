# Optional: SQL queries and the sample CSV work without credentials.
# Copy this file to .env in the project directory and fill in your key.
OPENAI_API_KEY=

# Set a model explicitly to use the same Ask configuration in CLI and server.
OPENAI_MODEL=

# Optional provider overrides.
# OPENAI_BASE_URL=https://your-provider.example/v1
# OPENAI_TIMEOUT_SECONDS=60
# OPENAI_JSON_MODE=true
